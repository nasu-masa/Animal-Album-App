# Docker Compose skeleton for local development.
# 実装フェーズでビルドコンテキスト・環境変数・ボリューム等を詰めていく想定のひな形です。

services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    # ports:
    #   - "3000:3000"
    # env_file:
    #   - ./frontend/.env
    # depends_on:
    #   - backend

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    # ports:
    #   - "8000:8000"
    # env_file:
    #   - ./backend/.env
    # depends_on:
    #   - db

  db:
    image: mysql:8.0
    # ports:
    #   - "3306:3306"
    # environment:
    #   MYSQL_DATABASE: cat_photo_app
    #   MYSQL_USER: app
    #   MYSQL_PASSWORD: secret
    #   MYSQL_ROOT_PASSWORD: root
    # volumes:
    #   - db-data:/var/lib/mysql

# volumes:
#   db-data:
